info:
  name: adcionar grão
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:5050/api/cafes
  body:
    type: json
    data: |-
      {
        "nome":"centro",
        "torra":"media",
        "disponivel":true,
        "quantidade": 4
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
